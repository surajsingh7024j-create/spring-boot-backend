apiVersion: v1
kind: Service
metadata:
  name: chatkarlore-service
spec:
  selector:
    app: chatkarlore
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080